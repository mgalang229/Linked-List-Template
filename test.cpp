#include <bits/stdc++.h>
#include "floatlist.h"
#include "implementation.cpp"

using namespace std;

int main() {
	FloatList <int> list;
	list.appendNode(199);
	list.appendNode(200);
	list.appendNode(100);
	list.displayList();
	return 0;
}
